# Indore Relay — frontend-only prototype
#
# No environment variables are required to run this version: admin
# credentials live in src/config/adminConfig.js and the video ID lives
# in src/config/videoConfig.js.
#
# These are placeholders for when the storage layer is swapped for a
# real backend (e.g. Supabase). Uncomment and fill in when that
# happens — nothing reads these yet.

# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
